// character/attributes.js

// Define the Attribute class to represent character attributes
class Attribute {
    constructor(name, value) {
        this.name = name; // Attribute name (e.g., Strength, Agility)
        this.value = value; // Attribute value (e.g., 10)
    }
}

// Export the Attribute class
export { Attribute };
